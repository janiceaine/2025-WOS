<style>
    .navbar-nav .nav-item:not(:last-child) .nav-link::after {
        content: "|";
        margin-left: 0.5rem;
        color: teal;
    }

    .navbar-nav .nav-link {
        text-decoration: underline;
    }
</style>

<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container">
        <img src="/images/Subject.png" alt="Logo" width="40" height="50" class="me-2"/>
        <a class="navbar-brand" href="/">The Groan Zone</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">

                @if (Context.Session.GetInt32(key: "userId") is not null)
                {
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Jokes" asp-action="JokesIndex">All Jokes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Jokes" asp-action="NewJokesForm">Add Jokes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Account" asp-action="me">Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Account" asp-action="ConfirmLogout">Sign out</a>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Account" asp-action="RegisterForm">Register</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Account" asp-action="LoginForm">Log In</a>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>