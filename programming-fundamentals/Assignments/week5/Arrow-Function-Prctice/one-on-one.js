const numbers = [5, 6, 7];
const nums = numbers.map(number => number + 1);
console.log(nums);

const animals = ["cat", "dog", "bird"];
const pets = animals.map(animal => animal.toUpperCase());
console.log(pets);

















